node -v

set PUPPETEER_SKIP_DOWNLOAD=true
npm install
npm install whatsapp-web.js
npm install puppeteer@latest
npm install qrcode-terminal
npm install node-cron

Notes:
whatsapp-web.js: WhatsApp Web client
qrcode-terminal: prints QR code in the console so you can scan

but for memory sake on render:

npm uninstall whatsapp-web.js puppeteer
npm install @whiskeysockets/baileys qrcode-terminal pino

i made some editions but kept the original structrue which uses whatsapp-web.js